{
    "name": "padosoft/askmydocs",
    "description": "AskMyDocs - Enterprise AI Knowledge Base (RAG) on Laravel.",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": "^8.3",
        "laravel/ai": "^0.6",
        "laravel/framework": "^13.0",
        "laravel/sanctum": "^4.2",
        "padosoft/laravel-ai-regolo": "^0.2",
        "padosoft/laravel-pii-redactor": "^1.1",
        "phpoffice/phpword": "^1.3",
        "smalot/pdfparser": "^2.10",
        "spatie/laravel-activitylog": "^4.12|^5.0",
        "spatie/laravel-permission": "^6.25",
        "symfony/yaml": "^7.4|^8.0"
    },
    "require-dev": {
        "laravel/mcp": "^0.7",
        "mockery/mockery": "^1.6",
        "orchestra/testbench": "^11.0",
        "padosoft/eval-harness": "^0.1.0",
        "padosoft/laravel-flow": "^0.1.0",
        "phpunit/phpunit": "^12.0"
    },
    "suggest": {
        "predis/predis": "^2.0 — required when QUEUE_CONNECTION=redis (used by kb:ingest-folder and POST /api/kb/ingest)",
        "laravel/horizon": "^5.0 — optional Redis queue dashboard for monitoring ingestion workers",
        "league/flysystem-aws-s3-v3": "^3.0 — required when KB_FILESYSTEM_DISK=s3",
        "laravel/mcp": "^0.7 — required when exposing the enterprise-kb MCP server (app/Mcp/Servers/KnowledgeBaseServer.php + 10 total tools under app/Mcp/Tools/: 5 retrieval + 5 canonical/promote)",
        "dompdf/dompdf": "^3.0 — required when ADMIN_PDF_ENGINE=dompdf (Phase G4 PDF export strategy)",
        "spatie/browsershot": "^4.0 — required when ADMIN_PDF_ENGINE=browsershot (Phase G4 PDF export strategy; also needs Node.js + Chromium on the host)"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/",
            "Database\\Seeders\\": "database/seeders/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        }
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "sort-packages": true,
        "allow-plugins": {
            "composer/package-versions-deprecated": true,
            "php-http/discovery": true
        }
    },
    "minimum-stability": "stable",
    "prefer-stable": true,
    "repositories": [
        {
            "type": "vcs",
            "url": "https://github.com/padosoft/eval-harness",
            "comment": "Pending Packagist submission — resolved via Git tag v0.1.0 (immutable, reproducible)."
        },
        {
            "type": "vcs",
            "url": "https://github.com/padosoft/laravel-flow",
            "comment": "Pending Packagist submission — resolved via Git tag v0.1.0 (immutable, reproducible)."
        },
        {
            "type": "vcs",
            "url": "https://github.com/padosoft/laravel-pii-redactor",
            "comment": "Pending Packagist submission — resolved via Git tags v1.x (v1.1.0 GA shipped 2026-05-03; load-bearing for chat / embedding-cache / insights redaction per W4.1)."
        }
    ]
}
