import { Placeholder } from './Placeholder';

export function ChatPlaceholder() {
    return (
        <Placeholder
            icon="Chat"
            title="Chat"
            phase="Phase E"
            description="The React chat UI ports from the design reference in the next PR. It'll include conversations, message threads with wikilink hovers, inline charts, voice input, and per-message ratings."
        />
    );
}
