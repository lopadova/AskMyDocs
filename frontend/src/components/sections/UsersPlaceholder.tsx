import { Placeholder } from './Placeholder';

export function UsersPlaceholder() {
    return (
        <Placeholder
            icon="Users"
            title="Users & Roles"
            phase="Phase F2"
            description="CRUD for users, Spatie-backed role management, per-project memberships with folder-glob + tag scopes, and the permission matrix."
        />
    );
}
