import { Placeholder } from './Placeholder';

export function DashboardPlaceholder() {
    return (
        <Placeholder
            icon="Grid"
            title="Admin Dashboard"
            phase="Phase F1"
            description="KPI cards, chat volume, token burn, rating distribution, slowest queries, health strip, and live activity feed — all refetched every 30 seconds via TanStack Query."
        />
    );
}
