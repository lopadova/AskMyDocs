import { Placeholder } from './Placeholder';

export function InsightsPlaceholder() {
    return (
        <Placeholder
            icon="Sparkles"
            title="AI Insights"
            phase="Phase I"
            description="Daily snapshots of promotion candidates, orphan documents, missing tags, coverage gaps, stale canonical docs, and quality reports with one-click actions."
        />
    );
}
