import { Placeholder } from './Placeholder';

export function KbPlaceholder() {
    return (
        <Placeholder
            icon="Book"
            title="Knowledge Base"
            phase="Phase G"
            description="Tree navigator, markdown preview with wikilinks and callouts, CodeMirror 6 source editor, graph view, ACL editor, and per-document history."
        />
    );
}
