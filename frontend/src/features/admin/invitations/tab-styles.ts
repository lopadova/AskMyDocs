import type { CSSProperties } from 'react';

/* Shared layout tokens for the Invitations tabs (keeps the filter rows uniform). */

export const filterRowStyle: CSSProperties = {
    display: 'flex',
    gap: 14,
    alignItems: 'flex-end',
    flexWrap: 'wrap',
    marginBottom: 12,
};
