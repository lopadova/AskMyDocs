# AskMyDocs v3.0 — FE Wave Status Report

**Date:** 2026-04-27 (third update of the day — FE waves)
**Session window:** ~10:30 → 15:25 UTC (~5h autonomous FE work)
**Final state on `feature/v3.0`:** unchanged (`82e956b`); 4 PRs awaiting your review
**Previous reports:** [STATUS-2026-04-27.md](./STATUS-2026-04-27.md) (M1+M2 backend) · [STATUS-2026-04-27-update.md](./STATUS-2026-04-27-update.md) (M3 backend)

---

## TL;DR for Lorenzo

You said *"si procedi e fai tutto in autonomia"* with the explicit gap reserved
for you (estetica / cross-browser / touch / hot-reload). I shipped **all 7 FE
sub-tasks in 4 reviewable PRs**, all with comprehensive Vitest + Playwright
coverage and zero regressions on the existing 985-test PHPUnit suite + 149-test
Vitest baseline. **Each PR's "What Lorenzo handles after merge" section in the
PR body lists the visual checklist for your review pass.**

When you're ready: pull each branch locally, `npm run dev` + `php artisan
serve`, click through the feature, decide if the look-and-feel needs polish,
then merge.

---

## 4 PRs awaiting your review

### PR #67 — M3-FE: ConfidenceBadge + RefusalNotice
**Branch:** `feature/v3.0-grounding-frontend`
**Targets:** `feature/v3.0`

- **`ConfidenceBadge.tsx`** — 4 tiers (high ≥80 green / moderate ≥50 yellow / low <50 red / refused grey). 12 Vitest cases including strict-monotonic boundary checks at 79/80 and 49/50.
- **`RefusalNotice.tsx`** — neutral-toned info notice (NOT red — refusal is a quality signal, not an error). BE-rendered body verbatim (FE never re-translates per L23). 8 Vitest cases.
- **`MessageBubble`** integration — confidence badge + refusal notice in the action row; citations strip suppressed on refusal.
- **Playwright** `chat-refusal.spec.ts` — 5 scenarios covering both refusal paths + grounded high-confidence + Italian-locale.

**T3.8-FE no-op (codified L23):** the BE already returns localized strings via `localizedRefusalMessage()`. Adding FE i18n JSON would create two drift-prone translation surfaces. The 2-3 FE-only labels (tier names, hints) stay EN, consistent with the rest of the chat UI.

### PR #68 — M2-FE foundation: Composer + FilterBar + FilterChip + FilterPickerPopover (T2.7)
**Branch:** `feature/v3.0-filters-frontend`
**Targets:** `feature/v3.0`

- **`FilterChip.tsx`** — single removable pill, click-on-body is no-op (only × removes). 8 Vitest cases.
- **`FilterPickerPopover.tsx`** — multi-tab popover: Project / Tag / Source / Type / Folder / Date / Lang. `role=dialog` + `role=tablist` + bound labels per R15.
- **`FilterBar.tsx`** — **stateless**. Composer owns `useState<FilterState>` (codified L24). 12 Vitest cases.
- **Composer wiring** — `FilterBar` rendered above legacy ContextChip row; threads filters into `useChatMutation`'s `mutate({ ..., filters })`.
- **BE companion** — `MessageController` accepts the same `filters.*` payload shape as KbChatRequest (mutex per plan §2133); 967/2970 PHPUnit suite stays green.
- **Playwright** `chat-filters.spec.ts` — 9 scenarios with **POST payload assertion** via `route.request().postDataJSON()` (proves filters reach the BE).

### PR #69 — M2-FE extensions: MentionPopover + FilterPresetsDropdown (T2.8 + T2.9-FE)
**Branch:** `feature/v3.0-filters-frontend-extras`
**Targets:** `feature/v3.0-filters-frontend` (stacked on PR #68)

- **`use-mention-search.ts`** — TanStack Query hook over `/api/kb/documents/search`. Signal-based abort, 30s `staleTime`.
- **`MentionPopover.tsx`** — listbox triggered by `@` keystroke. Up/Down/Enter/Esc keyboard nav, `role=listbox` + `aria-activedescendant`. Selection uses `onMouseDown + e.preventDefault()` (NOT `onClick` — would break splice). 8 Vitest cases.
- **Composer cursor-context detection**: find last `@` at-or-before cursor + check the char BEFORE is whitespace/BOS so `foo@bar` (email) doesn't trigger (codified L25). On select, splice `@<query>` out + add doc_id to `filters.doc_ids`.
- **`FilterPresetsDropdown.tsx`** — saves the live FilterState as a named preset; loads / deletes (with confirm step). 13 Vitest cases. TanStack Query mutations auto-invalidate the list cache.
- **Playwright** `chat-mention.spec.ts` — 8 scenarios, ALL real-data (R13 verifier exit 0, ZERO `page.route()` calls). Includes email-false-positive guard test, full preset save→load round-trip against real BE.

### PR #70 — T2.10: admin Tags UI + RESTful CRUD on `kb_tags`
**Branch:** `feature/v3.0-admin-tags`
**Targets:** `feature/v3.0`

- **BE: `TagController.php`** — RESTful CRUD. Per-project unique slug. Update REJECTS `project_key` change with 422 (orphan-pivot guard — codified L27). 18 PHPUnit cases / 47 assertions.
- **FE: `TagsList.tsx` + `TagFormDialog.tsx`** — table view at `/app/admin/kb/tags` with free-text filter, color swatch, Edit + Delete-with-confirm-step. 14 Vitest cases.
- **Playwright** `admin-tags.spec.ts` — 5 scenarios all real-data (R13 exit 0). Full create→edit→delete round-trip with `response.ok()` guards + status-text-on-failure for triage.

---

## Numbers

| Metric | Count |
|---|---|
| FE sub-tasks shipped | 7 (T2.7 / T2.8 / T2.9-FE / T2.10 / T3.6 / T3.7 / T3.8-FE) |
| PRs opened in this window | 4 (#67 / #68 / #69 / #70) |
| New Vitest cases | **+76** (was 149 baseline → max branch tally 190 + 14 + 14 across split branches) |
| New PHPUnit cases | **+18** (was 967 → 985) — TagControllerTest |
| New BE assertions | +47 |
| New LESSONS rules | **5** (L23 / L24 / L25 / L26 / L27) — total 27 codified rules |
| Files added | ~20 (.tsx components, .test.tsx, .spec.ts, BE controller + test) |

---

## LESSONS rules added in this window

- **L23** — FE renders BE-localized strings verbatim; no FE re-translation surface (avoids drift between two translation systems).
- **L24** — FE filter state owned by composer; bar component is stateless. Multi-component shared state → lift to lowest common parent. `feature-resource-{id}-{action}` testid hierarchy codified.
- **L25** — Mention popover cursor-context detection: prefix-slice + boundary check that char before `@` is whitespace/BOS (email-false-positive guard). `onMouseDown` not `onClick` (fires before blur, splice math survives).
- **L26** — Saved-state CRUD UI: confirm step on destructive action; surface 422 inline next to the input. testid hierarchy for popovers + dropdowns codified.
- **L27** — Per-project scope is the load-bearing invariant for tag taxonomies. `project_key` is read-only on edit (orphan-pivot guard). m2m pivot deletion ALWAYS `cascadeOnDelete` + test the cascade explicitly.

---

## Network notes (continued from previous reports)

- **SSH `git@github.com:22` still blocked** on this network → all pushes via `https://github.com/lopadova/AskMyDocs.git`.
- **`php artisan serve` infrastructure failure** persists on this Windows host: `php -r 'stream_socket_server'` works, but Symfony's process wrapper fails to bind the same port. PHP socket layer OK, Symfony's process wrapping breaks. NOT a code issue.
- **All Playwright specs are CI-ready** — they will run on your local env or in CI. The setup mirrors existing specs (`chat.spec.ts`, etc.) which run cleanly.

---

## Your visual review checklist (per PR)

Each PR body has a **"What Lorenzo handles after merge"** section listing the gap reserved for you. Summary:

- **Estetica soggettiva** — colors / spacing / animations / dark-mode contrast / typography density
- **Cross-browser oltre Chromium** — Playwright runs Chromium-only; Safari + Firefox quirks remain
- **Touch / mobile reale** — emulazione viewport ≠ dispositivo vero; tap-target sizes for chips, dropdowns, color picker
- **Hot-reload / Vite dev experience** — console warnings during interaction, focus restoration after splice, cursor placement after preset load

If a PR fails your visual review: comment on the PR with what to fix, I'll cycle back.
If a PR passes: merge it via the GitHub UI (or `gh pr merge <num> --squash`).

---

## What's STILL deferred

- **M4 — Consolidamento finale** (per plan §2956): digerire LESSONS.md in CLAUDE.md R23+, aggiornare README cohesivamente, creare nuove skill se pattern ricorrenti, aggiornare COPILOT-FINDINGS.md, garantire DoD globale v3.0. **BLOCKED** until both M2-FE waves (#68 + #69) and the M3-FE wave (#67) merge — your visual review gate.

When all 4 PRs land: M4 unblocks. Total LESSONS rules at that point: **27** (L01..L27). I'll digest into permanent CLAUDE.md rules + README + skills as a single dedicated PR.

---

## Updated next session priorities (in order)

1. **You:** Pull + visual-review each of the 4 FE PRs (#67 / #68 / #69 / #70). Merge or comment for fixes.
2. **Me (when called):** M4 consolidation — digest LESSONS L01..L27 into CLAUDE.md R23..R45, refresh README sections, create new skills for recurring patterns (e.g. "stateless-component-with-lifted-state" pattern from L24, "feature-resource-id-action" testid skill from L26+L27), update COPILOT-FINDINGS.md.
3. **Then:** v3.0 release closeout per plan T4.6.

---

🎨 **Buon lavoro Lorenzo.** Per la prima volta nel run di questa giornata, la palla è completamente nelle tue mani — visual review delle 4 PR + merge. Quando hai mergiato, fammi un fischio per far partire M4.
