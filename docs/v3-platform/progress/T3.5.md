# T3.5 Progress Log

**Status:** completed (PR #61 merged as d31444f)
**Started:** 2026-04-27 08:10:00 UTC
**Branch:** feature/v3.0-T3.5
**Plan reference:** docs/superpowers/plans/2026-04-26-v3.0-pipeline-filters-grounding.md (anchor: ### Task 3.5 — API response shape extension)

## Step Updates (append-only — newest at the BOTTOM)

### [08:10:00] Step 0: Pre-flight
**Action:** Branched off feature/v3.0-grounding-tier1 with T3.1+T3.2+T3.3+T3.4 + closeouts merged. T3.5 wires the ConfidenceCalculator (T3.2) into the controllers and extends the `/api/kb/chat` response shape with retrieval observability fields the dashboard needs.

### [08:15:00] Step 1: KbSearchService meta enrichment
**Action:** `KbSearchService::searchWithContext()` now emits richer `SearchResult.meta`:
- `retrieval_ms` — retrieval-only latency for the controller's breakdown.
- `search_strategy` — config snapshot ({semantic_enabled, fts_enabled, fusion_method, graph_expansion_enabled, rejected_injection_enabled, filters_applied}).
- `retrieval_stats` — per-query counts + score range ({candidates_pre_threshold (approx), candidates_post_threshold, primary_count, expanded_count, rejected_count, min_score_used, max_score_used}).

Two helpers introduced: `resolveFusionMethod()` returns `'rerank_weighted_sum'|'rrf'|'semantic_only'` based on config; `approxCandidatesPreThreshold($limit)` over-estimates candidate count via the rerank multiplier (avoids a hot-path `count()` round-trip — good enough for dashboard rollups).

### [08:20:00] Step 2: KbChatController extension
**Action:**
- Constructor-injected `ConfidenceCalculator $confidence`.
- After building citations: `$confidenceScore = $confidence->compute(primary, threshold, str_word_count(answer), count(citations))`. The citation count is by GROUPED documents, not raw chunks, matching what the user sees in the FE citations panel.
- Extracted `buildSuccessResponse()` helper composing the extended payload. **L21 — additive only**: `meta.latency_ms` stays a flat int, the breakdown lives under `meta.latency_ms_breakdown` as a sibling. New top-level `confidence` populated with the real score (was null in T3.3); `refusal_reason` still null on happy path.
- Refusal helpers (`refusalResponse` + `convertSentinelToRefusal`) updated to emit the SAME extended meta shape — search_strategy + retrieval_stats + latency_ms_breakdown — so the FE has a uniform JSON shape across grounded vs refused. LLM-side latency is `0` on the no-relevant-context path (no LLM call) and `total - retrieval` on the sentinel path.
- chat_logs row gets `confidence` in the `extra` JSON column (T3.1 column population is for messages, not chat_logs — the existing extra-blob path is the right hook).

### [08:25:00] Step 3: MessageController mirror
**Action:** Same ConfidenceCalculator wiring on the conversation flow. The flow uses `$search->search()` (flat collection, not searchWithContext) so retrieval_stats / search_strategy aren't available — confidence is fully comparable across both surfaces, but the meta surface is thinner. Documented in L21. The assistant message now persists confidence + refusal_reason on T3.1's dedicated columns AND in metadata (load-bearing for FE single-read rendering).

### [08:30:00] Step 4: Tests
**Action:** Created `tests/Feature/Api/KbChatResponseShapeTest.php` with 9 cases:

1. **Full extended structure assertion** — `assertJsonStructure` covering every new key.
2. **confidence is int 0..100** — type + range guard.
3. **confidence reflects retrieval quality** — three high-similarity chunks score ≥ 70.
4. **`meta.latency_ms` stays flat int** (L21 contract guard — defends the additive rule).
5. **`meta.latency_ms_breakdown` separates retrieval/llm/total** — total equals the flat int.
6. **`meta.search_strategy.fusion_method` + feature flags present** — config snapshot exposed.
7. **`meta.retrieval_stats` counts + scores match the SearchResult** — primary_count, candidates_pre_threshold, min/max scores.
8. **no_relevant_context refusal keeps extended meta shape** — confidence=0, but search_strategy + retrieval_stats + breakdown still present. LLM latency = 0.
9. **llm_self_refusal keeps extended meta shape** — confidence=0, retrieval_stats.primary_count populated, breakdown exists.

**Suite:** 9/9 / 82 assertions in 1.9s.

### [08:35:00] Step 5: Regression check
**Action:** Full PHPUnit run — **961/2952 green** (was 952/2870 → +9 tests / +82 assertions for T3.5, zero regression). All earlier T3.x tests still pass (T3.3 KbChatRefusalTest 9/9, T3.4 KbChatSentinelTest 7/7, T2.2 KbChatControllerFiltersTest 7/7).

## Verification Gate State
- [x] T3.5 own tests green (9/9 / 82 assertions)
- [x] No regression on T3.3 KbChatRefusalTest (9/9 still green)
- [x] No regression on T3.4 KbChatSentinelTest (7/7 still green)
- [x] No regression on T2.2 KbChatControllerFiltersTest (7/7 still green)
- [x] Full suite green (961/2952)
- [x] L21 additive-contract guard: `meta.latency_ms` stays int

## Completion Checklist (orchestrator-driven)
- [x] Verification Gate green
- [x] LESSONS.md entry appended (L21 — additive-only response shape)
- [x] Local commit made
- [x] Branch pushed (HTTPS)
- [x] PR opened with Copilot reviewer (#61)
- [x] Copilot review received (cycle-1 clean)
- [x] Fix cycle complete (no fixes needed)
- [x] PR merged (d31444f)
