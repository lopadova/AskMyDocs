# T3.3 Progress Log

**Status:** completed (PR #57 merged as 747a455)
**Started:** 2026-04-27 07:10:00 UTC
**Branch:** feature/v3.0-T3.3
**Plan reference:** docs/superpowers/plans/2026-04-26-v3.0-pipeline-filters-grounding.md (anchor: ### Task 3.3 — Refusal logic in KbChatController)

## Step Updates (append-only — newest at the BOTTOM)

### [07:10:00] Step 0: Pre-flight
**Action:** Branched off feature/v3.0-grounding-tier1 (T3.1+T3.2+T3.1-closeout merged). T3.3 implements the deterministic refusal short-circuit — the load-bearing invariant of the anti-hallucination tier.

### [07:15:00] Step 1: Config + i18n placeholder
**Action:**
- Added `kb.refusal.min_chunk_similarity` (default 0.45) + `kb.refusal.min_chunks_required` (default 1) to `config/kb.php`. Threshold intentionally above `default_min_similarity` (0.30) — the search step over-retrieves but the refusal step demands stronger evidence.
- Created `lang/en/kb.php` with the `no_grounded_answer` placeholder. T3.8-BE will expand it with `refusal.*` sub-keys.

### [07:20:00] Step 2: Controller short-circuit
**Action:**
- `KbChatController::__invoke()` — after `searchWithContext()`, filter `result.primary` by `vector_score >= refusalThreshold`. If the count is below `min_chunks_required`, return `refusalResponse()` BEFORE the AI call.
- `refusalResponse()` private helper persists the chat-log row (analytics need refusal turns to tune the threshold) with `aiProvider='none'`, `aiModel='none'`, `chunksCount=0`, and `extra: {refusal_reason, confidence: 0, ...}`. Returns `{answer: __('kb.no_grounded_answer'), citations: [], confidence: 0, refusal_reason: 'no_relevant_context', meta: {refused_early: true, ...}}`.
- Happy-path response shape extended with `confidence: null` + `refusal_reason: null` for FE shape uniformity.

### [07:25:00] Step 3: MessageController mirror
**Action:** `MessageController::store()` mirrors the same refusal logic for the conversation flow. Different shape because it works on a flat `$chunks` collection (`$search->search()`) rather than `SearchResult.primary`. Saves an assistant message with `confidence=0` + `refusal_reason='no_relevant_context'` directly on the `messages` table (T3.1 columns) AND in `metadata` JSON (legacy compatibility). Conversation gets `touch()` so the FE list reorders.

### [07:30:00] Step 4: TestCase lang_path fix
**Action:** Under Testbench, `__('kb.no_grounded_answer')` returned the raw key because the default `lang_path()` points at the Testbench skeleton. Added `$app->useLangPath(__DIR__.'/../lang')` in `tests/TestCase.php::getEnvironmentSetUp` so the project's `lang/` resolves naturally. Same `realpath()` fallback as `view.paths`.

### [07:35:00] Step 5: Tests
**Action:** Created `tests/Feature/Api/KbChatRefusalTest.php` with 9 cases:

1. **Zero primary chunks → refuse, no LLM call** — `shouldNotReceive('chat')` enforces the invariant.
2. **Chunks below threshold → refuse, no LLM call** — three chunks at 0.40, 0.35, 0.30 (all under 0.45).
3. **Chunk exactly at threshold → does NOT refuse** — boundary check (>= comparison).
4. **High-quality retrieval → does NOT refuse** — three chunks at 0.92/0.88/0.85.
5. **min_chunks_required=2 with one grounded → refuse** — count requirement enforced.
6. **Refusal answer is localized (not raw key)** — i18n smoke test.
7. **Refusal meta carries refused_early + zero counts + null provider/model**.
8. **Threshold is configurable via kb.refusal.min_chunk_similarity** — config knob works.
9. **Relaxed threshold lets low-score chunks ground answer**.

**Suite:** 9/9 / 30 assertions in 1.9s.

### [07:40:00] Step 6: Regression check
**Action:** Ran `phpunit --filter "KbChat"` — found 1 regression: `KbChatControllerFiltersTest::test_legacy_project_key_payload_still_works_and_wraps_into_filters` was using `primary: collect()` (empty) which now triggers refusal and zeroes `meta.provider`. Test's INTENT is filter threading, not refusal — fixed by adding a single high-similarity primary chunk to the mock. T2.2 test now passes the original assertion (`meta.provider === 'fake'`) AND the refusal short-circuit holds for empty-primary in T3.3 tests.

### [07:50:00] Step 7: Full suite verification
**Action:** Full PHPUnit run — **945/945 tests / 2845 assertions green**. Was 917/2775 at start of M3 → +28 tests / +70 assertions for T3.1+T3.2+T3.3.

## Verification Gate State
- [x] T3.3 own tests green (9/9 / 30 assertions)
- [x] No regression on KbChatControllerFiltersTest (T2.2 — 7/7 still green after mock update)
- [x] No regression on full suite (945/2845 green)
- [x] `Mockery::shouldNotReceive('chat')` enforces the no-LLM-call invariant
- [x] Both KbChatController + MessageController short-circuit on the same config keys
- [x] Refusal payload localized via `__('kb.no_grounded_answer')`

## Completion Checklist (orchestrator-driven)
- [x] Verification Gate green
- [x] LESSONS.md entry appended (L19 — `shouldNotReceive` + lang_path Testbench)
- [x] Local commit made
- [x] Branch pushed (HTTPS — SSH:22 blocked on this network)
- [x] PR opened with Copilot reviewer (#57)
- [x] Copilot review received (cycle-1 clean)
- [x] Fix cycle complete (no fixes needed)
- [x] PR merged (747a455)
