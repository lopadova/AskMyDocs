# T2.3 Progress Log

**Status:** completed
**Started:** 2026-04-27 05:10:00 UTC
**Last update:** 2026-04-27 05:33:00 UTC
**Branch:** feature/v3.0-T2.3
**Plan reference:** docs/superpowers/plans/2026-04-26-v3.0-pipeline-filters-grounding.md (anchor: ### Task 2.3 — Tag filter (whereHas tags with R19 escape))

## Step Updates (append-only — newest at the BOTTOM)

### [05:10:00] Step 0: Pre-flight
**Action:** Branch from feature/v3.0-filters-enterprise @ 08849e9 (T2.2 closeout). Verified `kb_tags` + `knowledge_document_tags` schemas exist (migrations under database/migrations/2026_04_23_000002,000003 + tests/database/migrations/0001_01_01_000014,000015). KbTag model exists. Will:
- Extend KbSearchService::applyFilters() with a whereExists subquery joining knowledge_document_tags + kb_tags by `kt.slug IN (...)`.
- Slugs are exact-match (no LIKE) so no R19 escape needed for slug values themselves — the test will document this so future maintainers don't try to "harden" with unnecessary escaping.
- Reflection-based SQL inspection tests per T2.1 LESSONS rule 1 (SQLite can't run pgvector hot path).
- Mutex zone: KbSearchService.php (T2.1 + T2.2 + T2.3 + T2.4 + T2.5 all touch it; serial execution preserves the mutex constraint).
**Result:** PASS

**Plan deviations identified:**
1. Plan §1822 has a "tag with name 'a_b' must NOT match documents tagged 'acb'" test — this is irrelevant since slug filtering uses `whereIn` (exact match), not LIKE. The R19 escape skill is for OTHER filter dimensions (LIKE-based searches). Documenting in LESSONS that slug-based filters are exact-match by design.
2. Tags are project-scoped (`uq_kb_tags_project_slug` allows the same slug across projects). The whereExists subquery joins via FK so the project boundary is enforced transitively (kt → kdt → knowledge_documents.project_key). No extra project constraint needed in the subquery itself — already covered by the chunk-level project_key whereIn from T2.1.

## Verification Gate State
- [ ] T2.3 own tests green
- [ ] All T2.1 + T2.2 tests still green (no regression on existing applyFilters dimensions)
- [ ] Full suite green

### [05:33:00] Step N: Implementation + cycle-1 + merge
**Cycles:**
- cycle-0 (19c61b2): tag filter implementation + 2 tests + LESSONS rule. Initial claim was "transitively enforced via FK chains" — this was over-confident.
- cycle-1 (19c61b2 → 0ec3c6c): 2 NEW (Copilot caught the over-confident transitivity claim — knowledge_document_tags pivot has no project_key FK so cross-project pollution is application-invariant, NOT schema-enforced). Fixed by adding explicit `whereColumn('kt.project_key', 'knowledge_chunks.project_key')` constraint + corrected LESSONS rule 2. Added 1 cycle-1 test pinning the project constraint.

**Final state:** PR #47 squash-merged as commit `8c9671a` on `feature/v3.0-filters-enterprise`. Suite pending full check after this commit.

## Completion Checklist (orchestrator-driven)
- [x] Verification Gate green (15 own tests / 50 assertions)
- [x] LESSONS.md entry appended (T2.3: slug-exact-not-LIKE rule + corrected tag-join project-scoping rule with the explicit-constraint approach for join-table filters)
- [x] README delta applied (none — already covered by T2.2 README)
- [x] Local commit made (cycle 0: 19c61b2, cycle 1: 0ec3c6c)
- [x] Branch pushed
- [x] PR opened with Copilot reviewer (PR#47)
- [x] Copilot review received (cycle 1: 2 NEW — bug + LESSONS correction)
- [x] Fix cycle complete (cycles: 1 — defensive constraint + doc rewrite; merged directly per revised policy)
- [x] PR merged + branch deleted (merge sha: 8c9671a)
