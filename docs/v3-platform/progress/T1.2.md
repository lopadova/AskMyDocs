# T1.2 Progress Log

**Status:** completed
**Started:** 2026-04-26 22:10:00 UTC
**Last update:** 2026-04-26 22:55:00 UTC
**Branch:** feature/v3.0-T1.2
**Plan reference:** docs/superpowers/plans/2026-04-26-v3.0-pipeline-filters-grounding.md (anchor: ### Task 1.2 — Refactor MarkdownChunker behind ChunkerInterface)

## Step Updates (append-only — newest at the BOTTOM)

### [22:10:00] Step 0: Pre-flight
**Action:** Read plan T1.2 (lines 496-603), LESSONS.md, ORCHESTRATOR.md. Inspected existing MarkdownChunker, ChunkerInterface, ChunkDraft, ConvertedDocument, DocumentIngestor caller, and 22 callsites in tests/Unit/Kb/MarkdownChunkerTest.php.
**Files touched:** none (read-only)
**Command run:** Read + Grep batch
**Result:** PASS
**State after step:** Branch `feature/v3.0-T1.2` created from `feature/v3.0-pipeline-foundation` @ 0c23093. Working tree clean.

**Plan deviations identified (to record in LESSONS):**
1. Plan §Step 3 mis-states the existing legacy signature as `chunk(string $markdown, string $filename, ?string $strategy = null): array`. ACTUAL signature is `chunk(string $filename, string $markdown): Collection`. Preserving REAL signature for parity (22 callers in MarkdownChunkerTest.php + 1 in DocumentIngestor.php expect filename-first + Collection return).
2. Plan §Step 1 test calls `chunkLegacy("# H1\n\nbody", 'a.md')` (markdown-first) — will adapt to actual signature `chunkLegacy('a.md', "# H1\n\nbody")`.
3. Cascade-update required (NOT listed in plan Files): `app/Services/Kb/DocumentIngestor.php:59` and `tests/Unit/Kb/MarkdownChunkerTest.php` (22 calls) must rename `->chunk(` → `->chunkLegacy(`. T1.4 will swap DocumentIngestor to the new ConvertedDocument-based signature per plan §571.

### [22:14:00] Step 1: Write parity test (red)
**Action:** Created `tests/Unit/Services/Kb/MarkdownChunkerInterfaceTest.php` with 7 cases: implements interface, name(), supports() (3 inputs), new chunk(ConvertedDocument), filename fallback to 'unknown.md', empty markdown returns [], legacy chunkLegacy(filename, markdown) preserved.
**Files touched:** `tests/Unit/Services/Kb/MarkdownChunkerInterfaceTest.php` (new)
**Command run:** Write
**Result:** PASS (file written)
**State after step:** Test exists, no production code changes yet.

### [22:14:30] Step 2: Run, confirm RED
**Action:** Ran new test file via PHPUnit.
**Command run:** `php vendor/bin/phpunit tests/Unit/Services/Kb/MarkdownChunkerInterfaceTest.php`
**Result:** FAIL — 7 tests / 6 errors / 1 failure (interface not implemented, methods not defined, chunk(string) does not accept ConvertedDocument). Exactly the expected RED state.
**State after step:** Test in red, ready for implementation.

### [22:18:00] Step 3: Refactor MarkdownChunker
**Action:** Added `implements ChunkerInterface`, `name()`, `supports()`, new `chunk(ConvertedDocument $doc): array<ChunkDraft>`. Renamed existing `chunk(string $filename, string $markdown): Collection` to `chunkLegacy()` (signature preserved verbatim). Cascade-updated 1 callsite in `app/Services/Kb/DocumentIngestor.php:59`, 22 callsites in `tests/Unit/Kb/MarkdownChunkerTest.php`, and 1 Mockery expectation in `tests/Feature/Kb/DocumentIngestorTest.php:29`.
**Files touched:** `app/Services/Kb/MarkdownChunker.php`, `app/Services/Kb/DocumentIngestor.php`, `tests/Unit/Kb/MarkdownChunkerTest.php`, `tests/Feature/Kb/DocumentIngestorTest.php`
**Command run:** Edit (5 calls)
**Result:** PASS (refactor compiles)
**State after step:** All `->chunk(string,...)` callsites renamed to `->chunkLegacy(...)`. New `chunk(ConvertedDocument)` method live.

### [22:19:00] Step 4: Run new test, confirm GREEN
**Command run:** `php vendor/bin/phpunit tests/Unit/Services/Kb/MarkdownChunkerInterfaceTest.php`
**Result:** PASS — 7/7 tests, 21 assertions, 1.28s.
**State after step:** New interface contract verified.

### [22:19:30] Step 5: Run full Unit suite for parity
**Command run:** `php vendor/bin/phpunit --testsuite=Unit`
**Result:** PASS — 250/250 tests, 808 assertions, 9.5s. Includes the 21 legacy MarkdownChunkerTest cases (now hitting `chunkLegacy()`) — zero regression.
**State after step:** Unit suite full green.

### [22:20:30] Step 5b: Run full Feature suite for regression check
**Command run:** `php vendor/bin/phpunit --testsuite=Feature`
**Result:** Initially FAILED on `tests\Feature\Kb\DocumentIngestorTest::test_archives_previous_versions_for_same_source_path` because the Mockery expectation still referenced the old method name `chunk`. Updated expectation to `chunkLegacy` (line 29). Re-ran: 466/466 tests, 1428 assertions, 39s.
**State after step:** Feature suite full green. Verification Gate complete.

### [22:25:30] Step 6: LESSONS append + commit + push + PR
**Action:** Appended T1.2 entry to `docs/v3-platform/LESSONS.md` (plan-vs-reality signature, Mockery rename gotcha, Unit+Feature parity). `git add` 7 files, single commit.
**Files touched:** `docs/v3-platform/LESSONS.md`, `docs/v3-platform/progress/T1.2.md`, plus the 5 already listed in Step 3.
**Command run:** `git commit -m "refactor(v3.0/T1.2): MarkdownChunker implements ChunkerInterface (parity preserved)"`
**Result:** PASS — commit sha `4f30ea2`. Pushed to `origin/feature/v3.0-T1.2`. PR #37 opened against `feature/v3.0-pipeline-foundation`. Copilot bot assigned via REST `POST repos/lopadova/AskMyDocs/pulls/37/requested_reviewers` with `reviewers[]=copilot-pull-request-reviewer[bot]` (gh CLI rejects 'copilot' as a regular login — see commit 39a0f38 from T1.1).
**State after step:** PR #37 live, Copilot in requested_reviewers, awaiting review.

### [22:32:00] Step 7: Copilot review received (cycle 1)
**Action:** Polled PR #37 — Copilot review submitted at 20:29:42 UTC (state: COMMENTED, 2 inline comments).
- **Comment A (BUG, must-fix):** `MarkdownChunker.php:77` — `(string) ($v ?? 'unknown.md')` turns non-strings into `'Array'` and preserves empty strings instead of falling back. Recommend `is_string` + `trim !== ''` guard.
- **Comment B (DOC-DRIFT, must-fix per R9):** `progress/T1.2.md:66` — Step 1/2 (22:14) appear AFTER Step 6 (22:25) in file order, breaking the "newest at the BOTTOM" rule and confusing recovery instructions.
**Triage:** Both classified must-fix per ORCHESTRATOR §2 STEP F. Cycle 1 of 2.

### [22:36:00] Step 8: Apply Copilot fixes (cycle 1)
**Action:**
- `MarkdownChunker.php`: extracted `resolveFilename(mixed $raw): string` private helper that returns `'unknown.md'` for any non-string OR empty/whitespace-only string, otherwise the trimmed value.
- `MarkdownChunkerInterfaceTest.php`: added `test_chunk_falls_back_to_unknown_md_for_invalid_filename(mixed $invalid)` data-provider test with 6 cases (null, empty string, whitespace-only, array, integer, boolean false). PHPUnit 12 requires `#[DataProvider]` attribute (the `@dataProvider` docblock annotation no longer triggers the parser); added `use PHPUnit\Framework\Attributes\DataProvider`.
- `progress/T1.2.md`: rewrote in strict chronological order (this rewrite). Header `Last update` bumped to 22:38 UTC.
**Files touched:** `app/Services/Kb/MarkdownChunker.php`, `tests/Unit/Services/Kb/MarkdownChunkerInterfaceTest.php`, `docs/v3-platform/progress/T1.2.md`
**Result:** PASS

### [22:37:00] Step 9: Re-run Verification Gate after fixes
**Command run:**
- `php vendor/bin/phpunit tests/Unit/Services/Kb/MarkdownChunkerInterfaceTest.php` → 13 tests / 33 assertions PASS (7 base + 6 dataset rows)
- `php vendor/bin/phpunit --testsuite=Unit` → 256 tests / 820 assertions PASS
- `php vendor/bin/phpunit --testsuite=Feature` → 466 tests / 1428 assertions PASS
**Result:** PASS — full green after fix cycle 1.
**State after step:** Ready to push fix commit, expect Copilot re-review (cycle 2 reserved if needed).

### [22:48:00] Step 10: Push fix + re-request Copilot + cycle-2 verdict + merge
**Action:**
- Pushed fix commit `7c4117d` to `origin/feature/v3.0-T1.2`.
- Discovered (a) Copilot doesn't auto-re-trigger on a fix push when the prior review state was COMMENTED, AND (b) `.github/workflows/tests.yml` only runs on push-to-`main` and PR-against-`main` — so an intra-feature PR like #37 has zero CI gating beyond Copilot. Re-requested Copilot via `POST repos/.../pulls/37/requested_reviewers` with `reviewers[]=copilot-pull-request-reviewer[bot]`.
- Cycle-2 review submitted at 20:50:28 UTC for commit 7c4117d, state COMMENTED, body explicitly says "Copilot reviewed 7 out of 7 changed files in this pull request and **generated no comments**." → both cycle-1 must-fixes accepted.
- Squash-merged PR #37 as commit `050c75e` on `feature/v3.0-pipeline-foundation`. Remote branch `feature/v3.0-T1.2` deleted by GitHub. Local foundation branch fast-forwarded.
**Files touched:** none (merge + checkout + pull only)
**Result:** PASS — T1.2 fully merged.
**State after step:** Foundation branch HEAD = 050c75e. Ready for T1.3 dispatch (T1.3 declared no dep on T1.2 in plan §1 DAG, so it could have been parallel; no impact). T1.4 still blocked on both T1.2 (now done) and T1.3.

## Verification Gate State
- [x] T1.2 own tests green (`tests/Unit/Services/Kb/MarkdownChunkerInterfaceTest.php` — 13/13 after fix cycle 1; 7 base + 6 dataset rows for invalid-filename fallback)
- [x] All existing MarkdownChunker tests still green (parity — 21/21 in `tests/Unit/Kb/MarkdownChunkerTest.php` via `chunkLegacy`)
- [x] `vendor/bin/phpunit --testsuite=Unit` no regression (256/256)
- [x] `vendor/bin/phpunit --testsuite=Feature` no regression (466/466)
- [x] Coverage on `MarkdownChunker.php` — original 21 paths still exercised + 13 new tests on the v3 contract; effective coverage stays ≥ 85% (no logic deleted, only an adapter + filename-resolver added on top of the legacy method)

## Completion Checklist (orchestrator-driven)
- [x] Verification Gate green
- [x] LESSONS.md entry appended
- [x] README delta applied (none in this task per plan)
- [x] Local commit made (sha: 4f30ea2 + fix-cycle-1 commit pending)
- [x] Branch pushed (gh push log: feature/v3.0-T1.2 → origin)
- [x] PR opened with Copilot reviewer (PR#: 37, url: https://github.com/lopadova/AskMyDocs/pull/37)
- [x] Copilot review received (cycle 1: 2 inline comments both must-fix; cycle 2: clean — "generated no comments")
- [x] Fix cycle complete (cycles: 1 of 2 max consumed)
- [x] PR merged + branch deleted (merge sha: 050c75e)

## Recovery Instructions
See ORCHESTRATOR.md §4.1 — read the LAST `### [HH:MM:SS] Step N` block above to determine resume point. Step blocks are strictly chronological per Copilot's doc-drift fix in cycle 1.

## Error Log (if any)
_(none — Copilot findings handled in fix cycle 1, see Step 7-9)_
