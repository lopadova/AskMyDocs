# T2.4 Progress Log

**Status:** completed
**Started:** 2026-04-27 05:35:00 UTC
**Last update:** 2026-04-27 06:00:00 UTC
**Branch:** feature/v3.0-T2.4
**Plan reference:** docs/superpowers/plans/2026-04-26-v3.0-pipeline-filters-grounding.md (anchor: ### Task 2.4 — Folder glob filter)

## Step Updates (append-only — newest at the BOTTOM)

### [05:35:00] Step 0: Pre-flight
**Action:** Branch from feature/v3.0-filters-enterprise @ 1c09d38 (T2.3 closeout). Read T2.4 plan + grep'd for existing matchesAnyGlob — found one in `app/Models/User.php:236` but it does NOT pass `FNM_PATHNAME` (an R19 violation in pre-existing code; out of T2.4 scope to fix). Will:
- Add `App\Support\KbPath::matchesAnyGlob(string $path, array $globs): bool` static helper with `FNM_PATHNAME` so `*` matches a single path segment and `**` crosses segments per R19.
- Apply the filter POST-FETCH in KbSearchService::search() (between candidate fetch and reranker), per plan §1879 — fnmatch isn't pgsql-native and globs with `**` don't map cleanly to LIKE.
- Reflection-based test for the helper + a minimal end-to-end test (still works because folderGlobs filtering happens in PHP, after the SQL — no pgvector dependency for the filter step).
- Mutex zone: KbSearchService.php (T2.4 → serial after T2.3).
**Result:** PASS

**Plan deviations identified:**
1. Plan said "applyFilters() handles it"; actually folderGlobs is applied AFTER applyFilters (post-fetch), per plan §1879. The applyFilters scaffold's "folderGlobs handled in T2.4" comment will be replaced with an explicit pointer to the post-fetch step.
2. KbPath::matchesAnyGlob is being added centrally; the existing duplicate in User.php (without FNM_PATHNAME) is a documented R19 violation worth a follow-up — flagging in LESSONS as a deferred fix so a future task picks it up.

## Verification Gate State
- [ ] T2.4 own tests green
- [ ] All T2.1+T2.2+T2.3 tests still green
- [ ] R19 invariant: `*` does NOT match `/` (FNM_PATHNAME)
- [ ] Full suite green

### [06:00:00] Step N: Implementation + cycle-1 + merge
**Cycles:**
- cycle-0 (38605b0): folder glob filter via KbPath::matchesAnyGlob (added) + post-fetch step in search() + 6 KbPath tests + LESSONS rule. Initial impl used fnmatch+FNM_PATHNAME; quickly discovered PHP fnmatch doesn't support `**` natively; replaced with glob→regex translator.
- cycle-1 (38605b0 → 9f542a3): 4 NEW (1 real bug — folder filter was applied BEFORE hybrid FTS merge, so FTS chunks bypassed the folder constraint; 1 coverage gap — extracted `filterByFolderGlobs` public method + 4 unit tests; 2 doc-drift — comments still referenced fnmatch when impl is glob→regex). Merged directly after cycle-1 push per revised policy.

**Final state:** PR #48 squash-merged as commit `cb8da85` on `feature/v3.0-filters-enterprise`. Suite at 887/2674.

## Completion Checklist (orchestrator-driven)
- [x] Verification Gate green (KbPath: 14 tests / 16 assertions; KbSearchServiceFilters: includes 4 new filterByFolderGlobs tests; full suite 887/2674)
- [x] LESSONS.md entry appended (T2.4: post-fetch trade-off + fnmatch ** limitation + R19 follow-up for User::matchesAnyGlob)
- [x] README delta applied (folder_globs section in T2.2's filter docs)
- [x] Local commit made (cycle 0: 38605b0, cycle 1: 9f542a3)
- [x] Branch pushed
- [x] PR opened with Copilot reviewer (PR#48)
- [x] Copilot review received (cycle 1: 4 NEW — bug + coverage + 2 doc)
- [x] Fix cycle complete (cycles: 1; merged directly per revised policy)
- [x] PR merged + branch deleted (merge sha: cb8da85)
