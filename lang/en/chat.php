<?php

declare(strict_types=1);

/**
 * User-visible chat strings. The machine-readable identifiers they
 * describe (e.g. `importance: 'critical'`) never localize — only these
 * bodies do (R24).
 */
return [
    'importance' => [
        'normal' => 'Normal',
        'high' => 'High',
        'critical' => 'Critical',
    ],
];
